# Edge TTS Local — Handoff

## 現況

- 2026-09-13 Computer Use 實機驗證完成：透過 @oai/sky 取得真實 collapsed / expanded 截圖，可直接看見後方文字穿透 surface；無原先方形底、雙框或異常弧線。展開兩列控制完整無裁切；實際 click 展開、收合成功，drag 前後座標由 (-493,1088) 變為 (-433,1113)，確認可拖曳。白色背景跨螢幕驗證未完成：工具禁止拖曳終點超出目標視窗 bounds，不宣稱已測白底。忙碌文字背景會透入，這是直接 alpha 透明，沒有模糊。
- 實機另發現啟動 StatusText 顯示 HTTP 502 Bad Gateway；此輪只驗證 UI，未宣稱語音上游正常。最早舊 PID 62916 不回應 --shutdown，經精確確認 command line 後只終止該 PID，未終止其他 dotnet/GPU 工作。最新 test PID 70864 可正常 --shutdown 結束；已重新用 start.ps1 啟動正常模式（local health ok）。正常模式 no-activate 未獨立驗證。

- 2026-09-13 最新 UI 修正：使用者新截圖仍見實心方底與雙重圓角。改用 WPF `AllowsTransparency=True` 的 per-pixel alpha，移除全部 DWM backdrop/frame/corner 設定，讓單一 24 DIP 圓角 surface 決定輪廓。底色 `#A6182232`（65% opacity / 35% 透光），文字／圖示保持不透明；移除 sheen、降低按鈕亮度並改為 16 DIP 圓角。這版是直接透明，不是 acrylic blur。
- 展開動作區由 88 增至 104 DIP，修正兩列按鈕因 MinHeight 加 Margin 超出列高而裁切。`scripts/check_overlay_visuals.ps1` 載入正式 XAML（隔離事件與服務），舊版先因 AllowsTransparency=False 失敗，修正版 collapsed/expanded 四角 alpha=0、surface alpha=166 通過，PNG 在 artifacts。已目視離屏渲染；未宣稱原生桌面疊合／滑鼠驗收。
- 本輪 Python 12/12、C# 14/14、Release build 0 warnings/errors；已用 app 自身 --shutdown 關閉舊版，start.ps1 啟動修正版正常模式，local service healthy。此紀錄取代前述待驗收的 DWM acrylic 方案與舊啟動阻擋現況。

- 2026-09-13：專案已由 `<projects>\edge-tts-local` 完整遷移至 `<projects>\tts`。目標原有且僅有的 `AGENTS.md` 已保留，其餘 13 個項目無碰撞搬入；舊來源資料夾已移除。
- 產品功能、429 修復與自動驗收完成；目前唯一交付阻擋是 Windows Smart App Control 的簽署要求不允許載入新建、未簽署的 WPF 組件。
- 使用者遇到的 429 已定位為本地容量錯誤，不是 Microsoft 上游：舊 50 ms semaphore 演算法在隔離 port、相同短句與 2 cancel + 2 replace 負載下共得到 40/40 個 429。修正版真實 Edge TTS 服務以相同負載得到 10/10 個 200（6.40 秒）。
- Backend 現在有 2 個 active synthesis slots、可取消且有界的 2 秒等待、明確 slot ownership 回收，以及一次 200–300 ms jitter retry。只有本地容量額滿會回帶 `X-Edge-Tts-Error: busy` 的 429；上游失敗會成為可讀的 502。Client 只對這個本地 busy 標記作兩次有界退避。
- Python backend 12/12 通過，包含 acquire 完成邊界取消、刻意在 acquire 完成與 `asyncio.wait` 返回之間取消、排隊中 disconnect、超過 2 請求依序成功、真正容量 429、timeout/retry/cache/validation。
- C# 13/13 通過，涵蓋文字/分段、中英術語、路徑/縮寫/版本、無空白中文 hard cap、依序且只播一次、replace、buffering pause、synth 中 skip、最多 2 並行/預取 3、rapid triple replace、失敗 retry/skip，以及只對帶本地 busy header 的 429 retry。
- NAudio 對真實 `smoke.mp3` 的 Stop→Play 驗收通過：`AUDIO RESTART PASS`。Release build 成功，0 warnings / 0 errors。主要組件為 `src/EdgeTtsOverlay/bin/Release/net8.0-windows/EdgeTtsOverlay.dll`。
- 已確認 `zh-TW-HsiaoChenNeural` 存在；先前固定非敏感中英例句的真實線上 smoke 為 2.50 秒、37,296 MP3 bytes，解碼 298,368 PCM bytes（24 kHz mono）。本輪未無故重複這項已通過的外部請求。
- UI 已針對使用者回報修正：移除造成方形突出感的全窗 `DwmExtendFrameIntoClientArea` 與貼滿視窗的裁切陰影；HWND 使用隨尺寸/DPI 更新的圓角 region，並請求 Windows 圓角；材質改為低 alpha acrylic tint、連續高光與不透明 fallback。這是 acrylic 背景模糊，不宣稱有光學折射。正常版仍保留 `WS_EX_NOACTIVATE` 與 `MA_NOACTIVATE`。
- Computer Use 的 `@oai/sky` 能列出測試視窗，但兩次 screenshot-only capture 都超過有界等待且沒有回傳 screenshot/state；已依 recovery 規則停止重試。因此修正版實際觀感、展開/滑鼠操作、正常版不搶焦點、托盤與多螢幕仍未完成目視驗收。
- `setup.ps1`、`test.ps1` 會檢查 native command exit code。`start.ps1` 相容 Windows PowerShell 5.1，建置後以 `dotnet.exe` 啟動 DLL，並等待 localhost health；若新程序非零退出會回報失敗，若單實例 IPC 成功而以 0 退出則回報既有實例已收到顯示要求。
- 2026-09-13 09:19（Asia/Taipei）最後啟動驗收：`dotnet EdgeTtsOverlay.dll` 及 `dotnet run --no-build` 都以 `FileLoadException 0x800711C7` 退出。Code Integrity Operational events 3033/3077 明確指出 `dotnet.exe` 載入 `EdgeTtsOverlay.dll` 時未達簽署層級，policy ID `{0283ac0f-fff1-49ae-ada1-8a933130cad6}`；Microsoft 文件將此 ID 列為 Windows inbox Smart App Control enforced policy。`CiTool -lp` 的唯讀查詢在本機回 `0x80070005`，因此沒有宣稱取得額外的 friendly-name 證據。未修改、停用或繞過系統政策。
- 搬移後 `.venv\Scripts\python.exe` 可正常 import `edge_tts` 與 `fastapi`；`pyvenv.cfg` 及 bash/cmd activation script 的舊路徑已更新。原始碼、腳本與文件沒有指向舊專案位置的絕對路徑；`obj` 中的舊路徑是建置快取，下一次 restore/build 會重建。
- 使用者搬移後回報「system block this command」的唯讀診斷：PowerShell policy 為 `LocalMachine=RemoteSigned`，其餘 scopes `Undefined`；`start.ps1` 只有 `:$DATA` stream，沒有 `Zone.Identifier`。它雖未簽署，但目前沒有 `tts\start.ps1` 的 Code Integrity 或 AppLocker 事件可證明腳本被擋。最近符合項目仍只有 09:08–09:19 舊路徑 `edge-tts-local` 的 EXE/DLL Code Integrity 3033/3077。尚缺使用者此次完整錯誤，故無法判定新回報發生在腳本入口或 DLL 載入階段；以既有證據，DLL 的 Smart App Control 阻擋仍是已確認問題。
- 先前的 Smart App Control 阻擋後已能以 process-scoped `RemoteSigned` 正常執行；沒有修改全機 ExecutionPolicy 或 Smart App Control。
- 2026-09-13 Computer Use 首次成功驗收後的修正：使用者確認方框突出已消失；本輪再降低 acrylic tint、加入外亮邊、內緣、頂部柔光及獨立內容層，並把 7 個展開動作改為兩列等寬控制，展開高度由 338 增為 372 DIP，改善標籤裁切與擁擠。正常版 no-activate 行為未更動；材質仍是 acrylic blur，不宣稱光學折射。
- 舊內建術語 `JSON → J SON` 已改為保留 `JSON`，載入設定時只遷移完全相同的舊預設值，其他使用者字典不變。新增實測 mixed fixture 與 `organizerJSON` 邊界驗證。
- 播放狀態新增可測的 `DisplayStatus`：暫停顯示「已暫停」，唯一項目完成顯示「已完成」，多項全完成顯示「就緒 · N 項已完成」。暫停按鈕的 glyph、tooltip 與 AutomationProperties.Name 會同步切換為「暫停朗讀／繼續朗讀」。
- 本輪驗證：Python backend 12/12、C# 14/14、Release build 0 warnings / 0 errors。
- 2026-09-13 Computer Use 修正版實際驗收：collapsed/expanded 都有圓角，acrylic 亮邊與玻璃層次可見，兩列按鈕沒有裁切；editor preview 保留 `JSON`，`API` 依預期以字母間隔呈現。Replace 的 2 段朗讀成功；Pause 顯示「已暫停」且按鈕 automation name 變為「繼續朗讀」；Resume 後顯示「已完成」；replay、stop、skip 均成功。`Ctrl+Alt+T` 隱藏成功，從 Notepad 作為 target 的全域快捷鍵顯示成功。
- 正常版不搶焦點仍未宣稱通過：show 後 Computer Use 的 `get_window_state` 錯誤擷取成 ChatGPT 且 focused 資訊為空，工具無法提供可靠證據。
- UI test 已由 app 自身 `--shutdown` 正常關閉（exit 0、關閉後 8766 listeners=0）。其後由 `powershell.exe -NoProfile -ExecutionPolicy RemoteSigned -File .\start.ps1` 啟動正常 no-activate 版；health=`ok`。再次執行入口回報既有實例收到顯示要求、exit 0，且 listener PID 前後皆為 64464，證明本輪維持單一服務實例。
- 目前交付狀態：正常版 overlay 保持運行，localhost `127.0.0.1:8766` health=`ok`，listener PID 64464。
- 2026-09-13 使用者截圖指出材質仍近乎實心且有異常線條。截圖與 XAML 對照確認：被 `ClipToBounds` 裁切的發光 `Ellipse` 形成右下弧／斜線，完整 inner rim 與外層 Border 形成雙框；這些視覺元素已移除。
- Acrylic 技術路徑已由 layered HWND + legacy `SetWindowCompositionAttribute` 改為 Windows 11 DWM system backdrop：`AllowsTransparency=False`、透明 composition target、full-client DWM frame、`DWMSBT_TRANSIENTWINDOW`、`DWMWA_BORDER_COLOR=COLOR_NONE` 與 DWM round corner。自訂 HRGN 也已移除。視覺層只保留單一垂直漸層外輪廓與不閉合的局部頂部 sheen，surface tint 降至約 9–19%。正常 no-activate/topmost/hotkeys 未變。
- 此次變更後自動驗證仍為 Python 12/12、C# 14/14、Release 0 warnings / 0 errors。System backdrop 的實際透光／模糊與 collapsed/expanded 邊緣需由本輪 Computer Use 在白色及彩色高對比背景再次確認，尚未先行宣稱通過。
- 上一筆「正常版保持運行」已被本輪開發取代：正常版已用 app 自身 `--shutdown` 關閉，修改後只會啟動 `--ui-test` 供視覺驗收。
- Computer Use 已確認 system backdrop 版有明顯透明度，怪弧／斜線與雙框都已消失；但 DWM 初次選成淺色 acrylic，使白色文字與 glyph 對比不合格。已在要求 backdrop 前設定 `DWMWA_USE_IMMERSIVE_DARK_MODE(20)=true`（舊版 attribute 19 fallback），保留低 alpha surface 與單一邊線。本次變更後 Python 12/12、C# 14/14、Release 0 warnings / 0 errors；深色 acrylic 的 collapsed/expanded 實際對比等待下一次 Computer Use 驗收，尚未先行宣稱通過。

## 下一步

1. 在使用者實際桌面確認透明度是否合意，以及拖曳／展開／正常 no-activate 行為；本輪已完成離屏 alpha 與版面驗證、正常啟動。
2. 若需發佈到其他受 Smart App Control 保護的電腦，仍需正規 code signing；目前本機可正常執行。

## 驗證命令

```powershell
.\test.ps1
dotnet run --project .\tests\EdgeTtsOverlay.Tests\EdgeTtsOverlay.Tests.csproj --no-restore -- --audio-restart .\smoke.mp3
.\.venv\Scripts\python.exe .\scripts\repro_429_legacy.py
.\.venv\Scripts\python.exe .\scripts\repro_429.py
```

## 從 HANDOFF 移下來的 2026-09-13 段落

### 2026-09-13（四）：清掉兩個已知 bug，然後開 repo 推上 GitHub

- **斷線不再噴 ASGI traceback**：`_run_cancellable`／`_synthesis_slot` 原本用 `CancelledError` 表示
  「client 走了」，uvicorn 當成未處理例外，每次 skip／replace 都印整段 traceback，會蓋掉
  `PythonServer.LastError` 的啟動診斷。改成自訂的 `ClientGone`，endpoint 收掉回 499。
  注意它必須在 `_synthesize` 的 `except Exception` **之前**重新丟出，否則會被當成上游失敗而重試再 502。
  回歸測試的 uvicorn 因此改回 `--log-level warning`——傳回噪音的話測試輸出會自己抓到。
- **MASTER.md 與程式碼同步**：不只材質描述（acrylic blur → per-pixel alpha，且現在沒有 opaque
  fallback），token 也多處過時，照 XAML 實際值更新：surface tint `#A6182232`、border `#38FFFFFF`、
  control surface `#10FFFFFF`／hover `#26FFFFFF`、圓角 24／16 DIP。
- **git 化**：`.gitignore` 排除 `bin/`、`obj/`、`.venv/`、`__pycache__/` 與整個 `artifacts/`。
  `artifacts/` 要排除是因為 `verify_*.png` 是實機截圖，會連帶拍到桌面上當時開著的東西。
  推之前掃過祕密（沒有，這工具本來就不需要 key）與本機絕對路徑（README 與封存的 HANDOFF 各兩處，已清）。

### 2026-09-13（三）：真連線下 /api/tts 會卡死——本輪最重要的修復

壓測時發現的，比原本在查的記憶體問題嚴重得多：**沒有併發、沒有斷線、一個正常會成功的請求，
也有約 17% 的機率讓 client 永遠收不到回應**（本機實測 7/40，卡住的請求 10 秒 timeout 還不回，
成功的只要 0.33 秒）。server 沒死、`/health` 正常、其他請求還能成功，就是「這一個」回應消失。
使用者端的表現就是按下朗讀之後沒反應、像當機。

根因在 `server/app.py` 的 `_wait_for_disconnect`：它輪詢 `request.is_disconnected()`，而 Starlette
那個 API 每次探測都把 ASGI `receive()` 包進一個微秒級的 `wait_for` 再取消掉——等於每秒 20 次去
中斷 uvicorn 進行中的 `receive()`，這個 race 會把連線卡住，讓合成好的回應永遠沒被 flush 出去。

改成直接等 ASGI channel（`await request.receive()` 直到 `http.disconnect`），不需要 timeout、
不需要輪詢、也就沒有取消 race。同一支重現腳本跑同樣 40 次：**修前 7 次卡死，修後 0 次**。

**為什麼 14 個綠燈測試漏掉它**：`server/test_app.py` 全部用假 Request，`receive()` 只是個普通
coroutine，根本碰不到真的 ASGI channel。所以新增了 `server/test_live.py`——起真的 uvicorn
（隔離 port 8799，`edge_tts` 照樣 mock 掉，不連 Microsoft），做假 Request 做不到的斷言：
25 次序列請求必須全部 200，以及中途斷線的請求不可以吃掉 synthesis slot。已掛進 `test.ps1`。

### 同一輪的記憶體修復

1. **per-segment CTS 洩漏**（`PlaybackCoordinator.PlayItemAsync`）：原本每個 segment 都
   `CreateLinkedTokenSource` 賦值給 `_segmentCts`，只有 finally 裡最後一個被 Dispose，所以 N 段
   文章會在 runToken 上累積 N 個 callback registration。改成建新的之前先 Dispose 前一個；
   `Skip()` 的 Cancel 因此要容忍 `ObjectDisposedException`（UI thread 與 playback thread 會搶）。
2. **Queue 無界成長**：每個 `ReadingItem` 帶 `OriginalText`（上限 20,000 字元）與 `Segments` 副本，
   長期 append 會一路累積。加了 `MaxQueuedItems = 200`，但**只丟已完成的最舊項目**——使用者排進來
   還沒聽的一律保留，即使因此超過上限（這是刻意行為，不是 bug）。
3. **temp mp3**：`Dispose()` 現在會 `CleanupTempFiles()`，不必等下次啟動才清。

### 壓測結果（sonnet 跑的，全部用 fake，沒打 Microsoft）

C# 六個情境全過，沒有未處理例外、沒有死結：4000 segment × 3 輪的長文章 heap 只 +6.3 KB 且中途
採樣不隨進度線性上升（CTS 洩漏修好的樣子）；50000 次零間隔 Skip() heap -96 B、handle -1；
10000 次併發 replace 只花 0.7 秒；append 洪水下 queue 穩在 200，全未讀的 100000 筆則一筆不少地留著。
Python 端 20000 次併發請求全 200、活躍合成數全程 ≤2、`_gate` 精準回到 2；5000 次中途斷線也沒有
slot 洩漏。工具留著：C# 是 `tests\...\Program.cs` 的 `--stress <scenario> <iterations>`，
Python 是 `server/stress_app.py`（要 RSS 數字的話 `pip install psutil`，它是選用的）。
