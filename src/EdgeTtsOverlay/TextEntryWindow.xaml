<Window x:Class="EdgeTtsOverlay.TextEntryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="朗讀文字" Width="700" Height="560" MinWidth="520" MinHeight="420"
        WindowStartupLocation="CenterOwner" FontFamily="Segoe UI Variable Text"
        Background="{StaticResource DialogSurface}" Foreground="{StaticResource TextPrimary}">
    <Grid Margin="22,18,22,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/><RowDefinition Height="140"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="原文" Style="{StaticResource SectionHeader}"/>
        <TextBox x:Name="OriginalBox" Grid.Row="1" Margin="0,0,0,14" AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" VerticalContentAlignment="Top" FontSize="14"
                 TextChanged="OnTextChanged" AutomationProperties.Name="原文編輯器"/>

        <TextBlock Grid.Row="2" Text="實際送讀預覽" Style="{StaticResource SectionHeader}"/>
        <TextBox x:Name="PreviewBox" Grid.Row="3" Margin="0,0,0,14" IsReadOnly="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" VerticalContentAlignment="Top"
                 Foreground="{StaticResource TextSecondary}" AutomationProperties.Name="實際送讀預覽"/>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock x:Name="CountText" VerticalAlignment="Center" Style="{StaticResource FieldLabel}"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="追加到佇列" MinWidth="104" Click="OnAppend" AutomationProperties.Name="追加至朗讀佇列"/>
                <Button Content="取代並朗讀" MinWidth="112" Margin="10,0,0,0" Background="#528FB7FF" BorderBrush="#85A9CCFF"
                        Click="OnReplace" AutomationProperties.Name="取代目前朗讀"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
